# .editorconfig
# This file is the top-most EditorConfig file
root = true

# Apply subsequent rules to all C# files
[*.cs]

#### Code Style Rules ####

# Require 'this.' qualifier for instance members to avoid ambiguity
dotnet_style_qualification_for_field = true:error
dotnet_style_qualification_for_property = true:error
dotnet_style_qualification_for_method = true:error
dotnet_style_qualification_for_event = true:error

# Prefer braces for all control flow statements
csharp_prefer_braces = true:error

# Use 'var' only when the type is apparent from the right-hand side
csharp_style_var_when_type_is_apparent = true:suggestion
csharp_style_var_for_built_in_types = false:error
csharp_style_var_elsewhere = false:error


#### Naming Convention Rules ####

# Example: Private fields must start with an underscore (_)
# 1. Define the symbol group
dotnet_naming_symbols.private_fields.applicable_kinds = field
dotnet_naming_symbols.private_fields.accessibility = private

# 2. Define the naming style
dotnet_naming_style.underscore_prefix.required_prefix = _
dotnet_naming_style.underscore_prefix.capitalization = camel_case

# 3. Create the rule that binds them and set its severity
dotnet_naming_rule.private_fields_must_have_underscore.symbols = private_fields
dotnet_naming_rule.private_fields_must_have_underscore.style = underscore_prefix
dotnet_naming_rule.private_fields_must_have_underscore.severity = error


#### .NET Code Quality Rules (CAxxxx) ####
# You can find a full list of rules here:
# https://learn.microsoft.com/en-us/dotnet/fundamentals/code-analysis/quality-rules/

# CA1062: Validate arguments of public methods
dotnet_diagnostic.CA1062.severity = error

# CA1822: Mark members as static if they don't access instance data
dotnet_diagnostic.CA1822.severity = warning

# CA2007: Do not directly await a Task without calling ConfigureAwait
dotnet_diagnostic.CA2007.severity = error

# CA1303: Do not pass literals as localized parameters
dotnet_diagnostic.CA1303.severity = suggestion